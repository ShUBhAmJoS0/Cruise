describe('Order Routes', () => {
  it('should have get all merchandise route', () => {
    expect(true).toBe(true);
  });

  it('should have mark order as complete route', () => {
    expect(true).toBe(true);
  });
});
